# $(basename "$f" .md | sed 's/_/ /g')

**Status (Jan 2026):** This document is retained for reference. The active pipeline is `create_abr_ladder.sh` with `create_hls_manifests.py` and `convert_to_segmentlist.py`.

See `generate_abr/README.md` for current usage.
